# Judge0 Configuration File
# This configuration is used internally by the self-hosted Judge0 Docker instance.
# No JUDGE0_API_KEY is required for EduMentorAI.

# Server Settings
PORT=2358
MAX_QUEUE_SIZE=100
MAX_CPU_TIME=10
MAX_REAL_TIME=15
MAX_MEMORY=512000
MAX_FILE_SIZE=65536
MAX_PROCESSES_AND_OR_THREADS=60
ENABLE_COMPILER_OPTIONS=true
